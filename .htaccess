# Root rewrite for portfolio
<IfModule mod_rewrite.c>
  RewriteEngine On

  # Redirect root /portfolio to /portfolio/public/
  RewriteCond %{REQUEST_URI} !^/portfolio/public/
  RewriteCond %{REQUEST_URI} !^/public/
  RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
